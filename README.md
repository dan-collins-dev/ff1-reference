# FF1 Reference API

## Project Overview

This is a learning based project that I have attempted several times, but let drop to the wayside. I have a little more time that I can use to get this project off the ground.

## Learning Goals
Using concepts learning while building a Web API in .NET and some concepts I wanted to dive deeper into, I want to accomplish the following:

- [ ] Improve the structure of my API projects
- [ ] Implement unit tests using Vitest
- [ ] Dip my toes in to Docker once the API is built
- [ ] Understand the relationships between the data and build a DB

## Running Locally

Required Tools

- VS Code
- NodeJS
- npm

In GitBash:

1. Verify NodeJS and npm are installed on your machine by running `node -v` and `npm -v`. If a version number is returned for each command, these have been successfully installed.
2. `cd` into the directory of your choice and run `git clone https://github.com/dan-collins-dev/ff1-reference.git`
3. `cd` into the cloned repo and run `npm install` to install the required packages.
4.  Open the project with `code .`
5.  Run the server with `npm run dev`

## For Those Interested

I'll be gathering data by playing the game with a 4 Monk party. Except for the bestiary, manually collected data will be in the order captured. I also did not go to Mt. Gulg once I obtained the Canoe.

I went to the Ice Cavern first, followed by the Citadel of Trials in order to unlock class upgrades early and obtain the airship sooner.

## Responsible AI Usage

- Used Copilot to generate basic tests for spells only. While there are plenty of free resources to get started writing unit tests, I haven't found one that goes into depth. Using AI to generate these tests helps provide me with more context when reading Vitest's documentation.